---
name: my-new-skill
description: Use when an agent needs to perform a specific repeatable workflow. Replace this with concrete trigger phrases, file types, systems, and outcomes.
---

# My New Skill

## Goal

Perform the workflow reliably with the fewest assumptions needed.

## Workflow

1. Inspect the user's inputs and identify the required output.
2. Read only the relevant files in this skill's `references/` directory.
3. Use scripts in `scripts/` when deterministic processing is needed.
4. Produce the requested artifact.
5. Verify the result through the closest available real surface.

## Resources

- Read `references/` only when the request needs detailed domain material.
- Use `scripts/` only after checking arguments and expected outputs.
- Use `assets/` as source material for generated artifacts.
